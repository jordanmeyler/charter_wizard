# Sprite sheets

Create a `SpriteSheet` asset here (`Create → Rune Magic → Sprite Sheet`, or `Window → Rune Magic → Sprite Sheet`) so Play can find clips by id.

Name clips `idle`, `walk`, `cast`, `melt`, `explode`. The library also registers `sheetId-clipName` (`adept-walk`, `ice-melt`).
